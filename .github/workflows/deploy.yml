name: Deploy

# Staging é o ambiente do PR: publica o código proposto antes do merge. O push em staging não
# dispara nada — depois do squash-merge para main, o back-merge de main em staging não traz
# conteúdo novo, e rodava o gate inteiro mais os cinco deploys para não mudar um arquivo.
#
# O PR mirando main entra aqui só pelo gate: a proteção da main exige os contextos `gate / quality`
# e `gate / integration`, que só existem se este workflow rodar no commit do PR. Ele não publica —
# staging já está publicada pelo PR que a alimentou.
on:
  push:
    branches: [main]
  pull_request:
    branches: [main, staging]
  workflow_dispatch:
    inputs:
      environment:
        description: Ambiente alvo do deploy manual
        required: true
        type: choice
        options: [staging, production]
      deploy_identity:
        description: Forçar o deploy do Keycloak (derruba o login por alguns segundos)
        required: false
        default: false
        type: boolean

permissions:
  contents: read

# Um grupo por PR: push novo no PR cancela o deploy anterior, que já é código velho em staging.
# `main` fica no grupo dele e nunca é cancelado — deploy de produção interrompido no meio deixa os
# cinco serviços em versões diferentes.
concurrency:
  group: deploy-${{ github.event_name == 'pull_request' && github.ref || github.ref_name }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  RAILWAY_CLI_VERSION: 4.58.0
  RAILWAY_PROJECT_ID: 62de4c69-216a-4335-93a0-4942c6a95c54

jobs:
  # Só há ambiente a resolver quando alguém publica: no PR de release este job e o `deploy` ficam de
  # fora, e sobra o `gate`, que é o que a proteção da main cobra.
  target:
    if: github.event_name != 'pull_request' || github.base_ref == 'staging'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      environment: ${{ steps.resolve.outputs.environment }}
    steps:
      - id: resolve
        run: |
          if [ -n "${{ inputs.environment }}" ]; then
            resolved="${{ inputs.environment }}"
          elif [ "${{ github.event_name }}" = "pull_request" ]; then
            resolved=staging
          elif [ "${{ github.ref_name }}" = "main" ]; then
            resolved=production
          else
            # Sem fallback: branch sem ambiente falha em vez de publicar em staging por engano.
            echo "Branch '${{ github.ref_name }}' não tem ambiente. Use workflow_dispatch." >&2
            exit 1
          fi
          echo "environment=$resolved" >>"$GITHUB_OUTPUT"
          echo "Ambiente alvo: $resolved"

  gate:
    uses: ./.github/workflows/ci.yml

  deploy:
    needs: [target, gate]
    runs-on: ubuntu-latest
    timeout-minutes: 45
    environment: ${{ needs.target.outputs.environment }}
    env:
      RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      TARGET_ENVIRONMENT: ${{ needs.target.outputs.environment }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - name: Install Railway CLI
        run: npm install --global "@railway/cli@$RAILWAY_CLI_VERSION"
      - name: Deploy identity
        run: |
          # Em PR o checkout é o merge commit: o primeiro pai é a base, e `event.before` não existe.
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            baseline="$(git rev-parse HEAD^1)"
          else
            baseline="${{ github.event.before }}"
          fi
          if [ "${{ inputs.deploy_identity }}" = "true" ]; then
            reason="pedido manual"
          elif ! .github/scripts/railway-deploy.sh has-succeeded keycloak; then
            reason="primeiro deploy do ambiente"
          elif git diff --name-only "$baseline" HEAD 2>/dev/null \
            | grep -q '^deploy/keycloak/'; then
            reason="mudança em deploy/keycloak"
          else
            echo "Keycloak inalterado: deploy pulado para não derrubar o login."
            exit 0
          fi
          echo "Deploy do Keycloak ($reason)."
          .github/scripts/railway-deploy.sh deploy keycloak
      # A API roda as migrations no preDeployCommand; worker e cron só sobem depois que ela conclui.
      - name: Deploy API
        run: .github/scripts/railway-deploy.sh deploy api
      - name: Conferir migrations aplicadas
        run: .github/scripts/railway-deploy.sh assert-migrations api
      - name: Deploy worker
        run: .github/scripts/railway-deploy.sh deploy worker
      - name: Deploy cron
        run: .github/scripts/railway-deploy.sh deploy cron
      # O trilho de NFS-e ainda não tem `NFSE_PROVIDER_BASE_URL_*` em produção — o endereço da
      # prefeitura é por instalação — e sem eles o processo falha no boot a cada janela. O passo
      # fica em staging até as duas URLs existirem lá.
      - name: Deploy cron-nfse
        if: needs.target.outputs.environment == 'staging'
        run: .github/scripts/railway-deploy.sh deploy cron-nfse
      - name: Deploy cron-notifications
        run: .github/scripts/railway-deploy.sh deploy cron-notifications
      - name: Deploy frontend
        run: .github/scripts/railway-deploy.sh deploy transportada-frontend
