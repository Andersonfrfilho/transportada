# Tasks — Importação agendada de NF-e (cron dedicado)

Referências: [spec.md](./spec.md) · [plan.md](./plan.md) ·
[ADR-0012](../../docs/adr/0012-scheduled-nfe-import-cron.md).

Ordem test-first: todo contract/migration-test vem **antes** da implementação
correspondente. `[P]` = paralelizável (não edita os mesmos arquivos). Task só
fecha com evidência registrada em `evidence.md`.

---

## Fase 0 — Migração de dados (ator sintético + origem de automação)

> 🤖 Modelo: `opus` (🧠 — migração fiscal crítica, CHECK + ator sintético + opt-in + 3 cópias de schema)

- [x] T001 Contract de migração/origem — `apps/api-transportada/test/nfe-schema/actor-origin.contract.ts` (novo) + registrado no entrypoint `test/nfe-schema.contract.test.ts` — assert estático: colunas `triggered_by`/`automation_job` presentes, ator NOT NULL, CHECK `nfe_imports_origin_ck`/`processing_outbox_origin_ck` de coerência. Runtime "insert viola CHECK" fica em T007. Evidência: vermelho antes da DDL (2 fail) → verde após (evidence.md).
- [x] T002 DDL migração `nfe_imports` — `apps/api-transportada/src/database/nfe.schema.ts` + `drizzle/20260726200123_scheduled_nfe_automation_origin/` — ADD `triggered_by text NOT NULL DEFAULT 'user'`, ADD `automation_job text`, ADD CHECK `nfe_imports_origin_ck`; **ator permanece NOT NULL**. Rollback ao lado. Verif.: `bun run db:check` verde.
- [x] T003 DDL migração `processing_outbox` — `apps/api-transportada/src/database/processing.schema.ts` (mesma migração) — espelha `triggered_by` + `automation_job` + CHECK `processing_outbox_origin_ck`; `actor_user_id` permanece NOT NULL. Verif.: `bun run db:check` verde.
- [x] T004 Nova tabela de opt-in — `apps/api-transportada/src/database/company-distribution-settings.schema.ts` (novo) + contract `test/fiscal-schema/company-distribution-settings.contract.ts` + **mesma migração** (`20260726200635_scheduled_nfe_automation_origin`, combinada com T002/T003) — `CREATE TABLE company_distribution_settings (company_id uuid PK/FK companies onDelete restrict, scheduled_distribution_enabled boolean NOT NULL DEFAULT false, created_at, updated_at)`. Verif.: `bun run db:check` verde + contract verde. Nota: FK `restrict` (estilo da casa), não CASCADE do rascunho do plano.
- [x] T005 Ator sintético + provisionamento — contract `test/companies/synthetic-actor.contract.ts` (vermelho primeiro, entrypoint `test/companies.contract.test.ts` + lista do `package.json`) + use-case idempotente `enable-scheduled-distribution.use-case.ts` (+ port `enable-scheduled-distribution.port.ts`): `execute({ companyId })` numa transação chama `ensureSystemActor` → `ensureCompanyMembership` → `enableScheduledDistribution`; UUID fixo `SYSTEM_DISTRIBUTION_ACTOR_USER_ID = '…006'` em `src/identity/domain/system-distribution-actor.constant.ts`. Um único `identity_users` de sistema é compartilhado; um `user_company_memberships` ativo por empresa; reexecução não duplica (5 testes verdes: provisiona, idempotente, ator compartilhado, escopo só da empresa, rollback ao falhar a flag). Filtro do UUID de sistema em consultas de usuários reais fica para quem expor o toggle (sem endpoint HTTP nesta feature — plan). Verif.: `bun test ./test/companies.contract.test.ts` = 5 pass / 0 fail; `typecheck` limpo.
- [x] T006 Ajustar inserts existentes da API — `src/nfe-imports/infrastructure/drizzle-nfe-import.repository.ts` (`createImport` → `insert(nfeImports)` e `saveOutbox` → `insert(processingOutbox)`) — `triggeredBy:'user'` explícito nas duas `.values()`; `automation_job` fica nulo (coerência do CHECK). `queueRetry` é UPDATE, herda a origem da criação — sem mudança. Verif.: typecheck limpo + regressão NF-e 56 pass / 0 fail (`nfe-import-application` + `nfe-schema` + `nfe-distribution-status` + `nfe-http`).
- [x] T007 `make migration-test` — ida+volta validada ao vivo em base descartável (10/10 checks): migração completa aplica (up); linha legada sem `triggered_by` recebe default `'user'` + `automation_job` null (CHECK ok); `automation` sem job e `user` com job → bloqueados por `nfe_imports_origin_ck`; `requested_by_user_id` nulo → not-null; `company_distribution_settings.scheduled_distribution_enabled` default false; `processing_outbox_origin_ck` presente; ator NOT NULL preservado nas duas tabelas; rollback (down) remove tabela + colunas de origem + entrada do journal. ⚠️ O alvo `make migration-test` (bundle `db:test`) fica **vermelho por drift pré-existente não-relacionado**: `LOCAL_IDENTITY_ROLES` foi expandido para 5 papéis (working tree) mas `local-identity-seed.integration.ts` ainda espera `['viewer']`. Fora do escopo da 011. Depende: T002–T006.

## Fase 1 — Sincronizar cópias de schema (worker + cron)

> 🤖 Modelo: `sonnet` (mecânico, mas verificação obrigatória de paridade)

- [x] T008 [P] Cópia worker — `apps/worker-transportada/src/database/processing.schema.ts` (+ `nfe.schema.ts`, cópia de `nfe_imports`) — aplicado `triggered_by` + `automation_job` nas duas cópias; ator NOT NULL preservado; worker só faz `.update(nfeImports)` (nunca insere nessas tabelas), origem herdada. `processing.schema.ts` (cópia minimalista): `text('triggered_by').notNull()` + `text('automation_job')`. `nfe.schema.ts` (cópia rica): `NFE_ORIGIN_TRIGGERS`/`NfeOriginTrigger` + `.$type<NfeOriginTrigger>().notNull().default('user')`. Verif.: typecheck limpo + `bun run --cwd apps/worker-transportada test` = 111 pass / 0 fail (inclui `nfe-distribution` + `outbox-relay`). Depende: T003.
- [x] T009 Scaffold `apps/cron-transportada` — `package.json` (lista explícita de testes, deps só Postgres: `drizzle-provider`/`drizzle-orm`/`logger`/`zod` — **sem** rabbitmq), `tsconfig` (NodeNext strict + exactOptionalPropertyTypes), `eslint.config.js`, `src/config/cron.constant.ts` (`CRON_JOBS`/`CRON_FISCAL_ENVIRONMENTS`/page-size), `src/config/cron.types.ts` (`CronEnvironment`/`CronLogger`), `src/config/environment.schema.ts` (zod: `DATABASE_URL`, `FISCAL_ENVIRONMENT`, `CRON_JOB`, `PAGE_SIZE`, `LOG_LEVEL`, `APP_ENV` — nome `.schema.ts` segue a convenção da casa vs. o literal `environment.ts` do plano), `src/logging/cycle-logger.service.ts` (`createCronLogger` + `createCycleContext`/`runWithCycleContext` → máscara `[traceId][ts][cron-transportada][JOB_ID]` via `@adatechnology/logger`), `src/main.ts` (entrypoint mínimo de scaffold: parse env + logger; T016 expande). Contract `test/environment.contract.test.ts` (5 casos: parse, default de page-size, job/ambiente inválidos rejeitados, sem vazar credencial no erro). App ligado aos scripts do root (build/lint/test/typecheck). Verif.: typecheck limpo + lint limpo + build ok + `test` = 5 pass / 0 fail.
- [x] T010 Cópias de schema do cron — `src/database/{nfe,processing,company-distribution-settings,distribution-cursor,digital-certificate,identity}.schema.ts` — cópias standalone (colunas + defaults + `$type`, sem FK/CHECK, estilo do worker; cron não roda migration). `nfe.schema` = `nfe_imports` (write enqueue), `processing.schema` = `processing_outbox` (write enqueue, `triggered_by`/`automation_job`), `company-distribution-settings` (read flag), `distribution-cursor` = `nfe_distribution_cursors` (read cooldown `next_allowed_at`), `identity` = companies + identity_users + user_company_memberships (membership sintético para leitura, alvos de FK do insert), `digital-certificate` (read status+janela p/ elegibilidade "certificado válido" — `secret_envelope` nunca selecionado). Paridade com as fontes da API. **Cópias extras além da lista literal** (`digital-certificate`, `identity`) justificadas pelas regras de elegibilidade T011. Verif.: typecheck + lint + test limpos. Depende: T002–T005, T009.

## Fase 2 — Job de puxada (lógica pura, contract-first)

> 🤖 Modelo: `opus` (🧠 — concorrência/advisory lock, idempotência, tenant; degradar para `sonnet` só nas queries triviais)

- [x] T011 Contract elegibilidade — `test/nfe-distribution-pull/eligibility.contract.ts` (novo) + entrypoint `test/nfe-distribution-pull.contract.test.ts` + lista do `package.json`. 11 casos: inclui elegível; carrega `environment` no resultado; pula cooldown (`next_allowed_at > now`) e inclui cooldown já vencido; pula flag `false`; pula sem certificado ativo (retired/ausente) e fora da janela (expirado/ainda-não-válido no instante do ciclo); pula sem membership sintético; pula empresa `disabled`; mistura → só elegíveis. Vermelho antes de T014 (módulo ausente) → verde depois.
- [x] T012 Contract enqueue — `test/nfe-distribution-pull/enqueue.contract.ts` (novo) + entrypoint. 5 casos: plano de automação (import `source=distribution`/`triggered_by=automation`/`automation_job=nfe.distribution.pull`/`requested_by=ator sintético`/`status=queued`, outbox `event_type=transportada.nfe.distribution.requested`/`aggregate_type=nfe_import`/mesmos campos de automação); coerência `import.id === outbox.aggregate_id === outbox.payload.importId`, `event_version=1n`, `received_count=0n`; `idempotency_key` deriva do bucket (empresa+environment+cadência) e é gravada no import; duplicado no mesmo bucket → `enqueued=false`, um só plano; bucket vira → key nova. Vermelho antes de T015 → verde depois.
- [x] T013 Contract concorrência — `test/nfe-distribution-pull/concurrency.contract.ts` — duas execuções simultâneas, só uma obtém `pg_try_advisory_lock`; a outra sai limpa (exit 0, zero enfileiramentos); ciclos sequenciais ambos adquirem (2º é duplicado idempotente); falha isolada por empresa. Verif.: typecheck + lint limpos, 3/3 pass (suíte cron 24/24). Vermelho antes de T016 → verde.
- [x] T014 `select-eligible-companies.use-case.ts` + `select-eligible-companies.port.ts` + `domain/distribution-eligibility.policy.ts` — predicado puro de elegibilidade (empresa ativa + flag + membership sintético + certificado ativo na janela + fora do cooldown) sobre candidatos do port `DistributionCandidateSourcePort`; use-case devolve `{companyId, environment}` só dos elegíveis, `companyId` sempre vindo da fonte (banco), nunca de payload. Fonte SQL Drizzle (leitura) fica no wiring do T016. Verif.: typecheck + lint limpos, 11/11 pass. Verde: T011.
- [x] T015 `enqueue-distribution.use-case.ts` + `enqueue-distribution.port.ts` + `domain/{distribution-pull.constant,distribution-idempotency.policy,system-distribution-actor.constant}.ts` — monta o `DistributionEnqueuePlan` (import + outbox de automação, ator sintético `…006`, `payload.importId=import.id`, espelhando o `saveOutbox` da API: `aggregate_type=nfe_import`, `event_version=1n`, envelope `{importId}`), delega ao gateway transacional `DistributionEnqueueGatewayPort.persist`; `idempotency_key` truncada à cadência (`Math.floor(now/bucketMs)*bucketMs`), `requestFingerprint=idempotencyKey`. Gateway Drizzle real (transação + captura de `23505` → `enqueued=false`) fica no wiring do T016. Verif.: 5/5 pass, typecheck + lint limpos. Verde: T012.
- [x] T016 `run-cycle.ts` + `job-registry.ts` + `nfe-distribution-pull.job.ts` + `main.ts` + adapters Drizzle (`infrastructure/{drizzle-advisory-lock,drizzle-distribution-candidate.source,drizzle-distribution-enqueue.gateway,crypto-identifiers}.ts`) — advisory lock de sessão via `pg_try_advisory_lock(hashtextextended('cron:'||JOB_ID,0))`, unlock no `finally`; conexão fixada em `max:1` (o lock atravessa as transações por empresa); source lê `company_distribution_settings ⋈ companies ⋈ membership(ator …006) ⋈ certificado ativo ⋈ cursor(ambiente)` e loga `evaluatedCount`; gateway insere import+outbox numa transação e captura `23505` → `enqueued=false`; `main` roda o ciclo dentro do trace de ciclo, exit 1 só se `failedCount>0`. Verif.: typecheck + lint limpos, suíte cron 24/24 pass. Verde: T013.

## Fase 3 — Integração, gates e infra

> 🤖 Modelo: `sonnet` (integração e infra reversível)

- [x] T017 Regressão relay→consumer — bloco `automation-origin distribution regression (T017)` em `apps/worker-transportada/test/outbox-relay.contract.test.ts`: linha de outbox de automação (ator sintético `…006`, `event_type=transportada.nfe.distribution.requested`, `aggregate_id=importId`) é reivindicada+publicada pelo relay **sem alteração** na topologia de distribuição, e o envelope resultante passa no gate exato do consumer (`nfeProcessingEnvelopeV1Schema.parse` + `type=distribution.requested`, `payload.importId=aggregateId`). O relay ignora `triggered_by`/`automation_job` (seleciona colunas explícitas). Verif.: worker typecheck + lint limpos, suíte contract worker 112/112 pass. Depende: T008, T015.
- [x] T018 [P] Manifesto K8s CronJob — `deploy/cron/nfe-distribution-pull.cronjob.yaml` (ConfigMap `cron-transportada-config` + CronJob `cron-transportada-nfe-distribution-pull`) — invoca `CRON_JOB=nfe.distribution.pull`, `schedule "0 * * * *"` espelhando `CADENCE_MINUTES=60` (regra de coerência documentada na spec, "## Configuração e cadência"), `timeZone America/Sao_Paulo`, `concurrencyPolicy Forbid` + `backoffLimit 0` + `restartPolicy Never` (janela idempotente, sem retry intra-ciclo), `DATABASE_URL` só via `secretKeyRef` (nunca em ConfigMap nem log), `FISCAL_ENVIRONMENT` explícito, hardening (runAsNonRoot/10001, readOnlyRootFilesystem, allowPrivilegeEscalation false, drop ALL, seccomp RuntimeDefault). Verif.: `Bun.YAML.parse` (2 docs OK) + lint offline de invariantes (cadência↔schedule, sourcing de segredo, hardening) = "manifest lint OK — 2 docs, all invariants pass". kubeconform/kubectl dry-run é o linter de CI pretendido, indisponível offline (cluster inacessível).
- [x] T019 `make check` completo — gate total verde (`MAKE_CHECK_EXIT=0`): format:check limpo (após `bun run format` repo-wide), lint das 4 apps limpo, typecheck limpo, test (api 486 pass/39 files, worker 112, cron 24, frontend 109, root 6), build das 4 apps. As três cópias de schema (`api/src/database`, `worker/src/database`, `cron/src/database`) sincronizadas — paridade validada pelo typecheck + suítes. Além da 011: corrigidos 3 erros pré-existentes de WIP não commitado no frontend que bloqueavam o gate (import não usado em `companySettingsClient.service.ts`, `no-misused-promises` em `CompanySettingsForm.component.tsx`, e regressão do `clearToken` no `logout` de `KeycloakAuthProvider.provider.ts` — conforme decisão do usuário). Depende: todas.
- [x] T020 `evidence.md` — consolidado T001–T007 (Fase 0), T008–T010 (Fase 1), T011–T016 (Fase 2), T017–T019 (Fase 3) + seção de encerramento da feature. Feature 011 entregue: cron Postgres-only só enfileira, relay+consumer existentes processam sem alteração, gate `make check` verde. Nada commitado (aguardando pedido explícito). Depende: T019.

---

## Pendências não bloqueantes a fechar durante a Fase 0/2

- Cadência default do CronJob (documentar na spec antes de T018).
- `text`+CHECK vs `pgEnum` para `triggered_by` — decidido `text`+CHECK (menos migração, alinhado ao existente); revisar em T002 se houver pgEnum já em uso no schema.
- UUID fixo do ator sintético de sistema — definir em `*.constant.ts` e garantir que consultas de usuários reais o filtrem (fechar em T005).
- Derivação exata do bucket de idempotência (truncamento à cadência) — fechar em T015 com base na cadência de T018.
