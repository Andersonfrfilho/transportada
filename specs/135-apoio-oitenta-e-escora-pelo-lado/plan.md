# Spec 135 — Plano

> 🤖 Modelo: `opus` 🧠 (D1–D3) · `sonnet` (D4, contratos, docs)

1. Juiz primeiro: a escora pelo lado em `unloading-simulation.ts`, e o contrato próprio dela
   (`brace-rule.contract.ts`).
2. Empacotador: bordas reais (`cargo-edge-grid.ts`), a mesma escora em `isConfined` (`isOwnStack`), e o
   apoio de 80% em `seat` e no complemento (`isRestable`).
3. Medir as quatro viagens, os cubos da 130, as 32 variações e o sintético de 85 paradas contra
   `ce0a2d08`.
4. O sintético: achar a causa por medição (lado, planta, relevo) e só então a regra (D3).
5. Tempo: perfil de CPU, e só otimização que mantém a assinatura das 99 cargas.
6. Docs, CLAUDE.md e gates.
