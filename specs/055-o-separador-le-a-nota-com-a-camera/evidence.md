# 055 — O separador lê a nota com a câmera · evidências

Uma entrada por task concluída: comando executado, saída relevante e o que ela prova.

| Task | Comando                                                                                                                                                                                                   | Resultado                                                                                                                                                                                                                                                                        |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T001 | `bun test apps/api-transportada/test/authorization.contract.test.ts apps/api-transportada/test/trip-http.contract.test.ts` (antes)                                                                        | `3 fail` — `trip.manage` ausente do catálogo, e as cinco rotas de escrita ainda abriam com `fleet.manage`                                                                                                                                                                        |
| T001 | `bun run --cwd apps/api-transportada test`                                                                                                                                                                | `2890 pass · 15 skip · 0 fail` em 118 arquivos — as cinco rotas exigem `trip.manage`, `company-admin` e `operator` a têm, os papéis de campo não, e o manifesto segue em `mdfe.manage`                                                                                           |
| T001 | `bun run typecheck`                                                                                                                                                                                       | as quatro apps sem erro — catálogo do frontend inclui `trip.manage`                                                                                                                                                                                                              |
| T001 | `bun test ./apps/frontend-transportada/test/trip.contract.test.ts` (antes)                                                                                                                                | `1 fail` — o portão da tela ainda era `fleet.manage`, e um controlador com `fleet.read`+`fleet.manage` continuava com `canManageTrips === true`                                                                                                                                  |
| T001 | `bun run --cwd apps/frontend-transportada test`                                                                                                                                                           | `1830 pass · 0 fail` em 18 arquivos — a tela abre a escrita para `trip.manage` e a fecha para quem só administra frota                                                                                                                                                           |
| T002 | `bun test ./apps/api-transportada/test/separator-role.contract.test.ts ./apps/api-transportada/test/authorization.contract.test.ts ./apps/api-transportada/test/identity-schema.contract.test.ts` (antes) | `19 pass · 8 fail` — `separator` fora de `COMPANY_ROLES`, fora do CHECK e sem conjunto de permissões                                                                                                                                                                             |
| T002 | `bun test ./apps/api-transportada/test/separator-role.contract.test.ts`                                                                                                                                   | `27 pass · 0 fail` nas três suítes — o papel alcança as cinco rotas de viagem, `GET /nfe-documents*` e as leituras de frota, e é recusado nas quatorze rotas de escrita de frota, faturamento e CT-e, mais `POST /trips/:id/mdfe-manifests`                                      |
| T002 | `bun run --cwd apps/api-transportada test`                                                                                                                                                                | `2893 pass · 15 skip · 0 fail` em 119 arquivos — inclui o CHECK inline de `membership_roles` e o derivado de `user_invitation_roles` com o oitavo papel                                                                                                                          |
| T002 | `bun run typecheck`                                                                                                                                                                                       | as quatro apps sem erro                                                                                                                                                                                                                                                          |
| T002 | `make migration-test`                                                                                                                                                                                     | `82 pass · 0 fail` — `20260824184702_separator_role` aplica, aceita `insert into membership_roles … 'separator'`, e o `rollback.sql` estreita os dois CHECKs e apaga exatamente uma linha do diário (o teste roda todos os rollbacks em ordem inversa e confere o diário no fim) |
| T003 | `bun test ./apps/frontend-transportada/test/shared.contract.test.ts` (antes)                                                                                                                              | `164 pass · 1 fail` — com `camera=()` no `server.ts`, o contrato recusa a política que fecha a câmera para a própria origem                                                                                                                                                      |
| T003 | `bun test ./apps/frontend-transportada/test/shared.contract.test.ts` (com `geolocation=(self)` temporário)                                                                                                | `164 pass · 1 fail` — a outra metade do contrato: capacidade de dispositivo que se abre de carona também reprova                                                                                                                                                                 |
| T003 | `bun test ./apps/frontend-transportada/test/shared.contract.test.ts`                                                                                                                                      | `165 pass · 0 fail` — `camera=(self), geolocation=(), microphone=()`, e a CSP mais os outros três cabeçalhos idênticos                                                                                                                                                           |
| T003 | `bun run --cwd apps/frontend-transportada test`                                                                                                                                                           | `1832 pass · 0 fail` em 18 arquivos — inclui `frontend-contract`, que cobra a paridade de `COMPANY_ROLES` com a API e obrigou o `separator` do T002 a chegar às duas cópias por valor do frontend e aos dois `identity*.locale.json`                                             |
| T003 | `bun run --cwd apps/frontend-transportada typecheck`                                                                                                                                                      | sem erro                                                                                                                                                                                                                                                                         |
| T003 | `bun run --cwd apps/frontend-transportada build`                                                                                                                                                          | build limpo — PWA v1.3.0 `generateSW`, 12 entradas de precache, `dist/sw.js` gerado, e `dist/content-security-policy.txt` emitido pelo plugin (o `server.ts` falha fechado sem ele)                                                                                              |

| T004 | `bun test test/shared.contract.test.ts` (antes) | `0 pass · 1 fail · 1 error` — `Cannot find module '@/modules/shared/nfeAccessKey.service'`: o contrato existe e o serviço não |
| T004 | `bun test test/shared.contract.test.ts` | `189 pass · 0 fail` — dígito cru, `p=` com e sem `\|`, `chNFe=` em três caixas, minúscula canonicalizada, CNPJ alfanumérico aceito, 43 e 45 posições recusadas, e o `p=` vencendo o `chNFe=` na mesma URL |
| T004 | `bun run --cwd apps/frontend-transportada test` | `1856 pass · 0 fail` em 18 arquivos |
| T004 | `bun run --cwd apps/frontend-transportada typecheck` | sem erro |
| T005 | `bun test test/nfe-documents.contract.test.ts test/nfe-schema.contract.test.ts` (antes) | `2 pass · 7 fail · 1 error` — `buildDocumentListFilters` não existia, e o `?accessKey=` caía no allowlist de `parseCursorPage` (os dois que já passavam eram justamente a chave malformada e a repetida, recusadas pelo allowlist antigo) |
| T005 | `bun test test/nfe-documents.contract.test.ts test/nfe-schema.contract.test.ts` | `32 pass · 0 fail` — chave levada à camada de aplicação ao lado da página do tenant, emitente alfanumérico aceito, minúscula canonicalizada, ausência vira `accessKey: null`, sete formas malformadas e a chave repetida viram `400` sem chamar a aplicação, e a chave de outra empresa sai como página vazia |
| T005 | `bun run --cwd apps/api-transportada test` | `2904 pass · 15 skip · 0 fail` em 120 arquivos — inclui as três novas asserções de tenant na query: o `company_id` é o primeiro filtro com e sem cursor, e nunca é substituído pelo filtro de chave |
| T005 | `bun run typecheck` | as quatro apps sem erro |
| T005 | `bun run lint` · `bun run format:check` | limpo nas quatro apps; o `format:check` da raiz reprovou 12 arquivos (a migration do T002, o contrato do T003 e os `.md` das specs) e passou depois do `prettier --write` |
| T006 | `bun run --cwd apps/frontend-transportada test test/trip.contract.test.ts` (fase vermelha) | `23 pass · 8 fail` — as oito falhas são `client.findNfeDocumentByAccessKey is not a function`, com o contrato escrito antes do método existir |
| T006 | `bun run --cwd apps/frontend-transportada test test/trip.contract.test.ts` (fase verde) | `31 pass · 0 fail` — a consulta sai como `GET /nfe-documents?limit=1&accessKey=…` com `Bearer` e `cache: no-store`; coluna nova na linha servida não quebra a leitura; página vazia devolve `null`; formato fora do contrato cai em `TRIP_RESPONSE_INVALID`; `AbortError` sobe como cancelamento e não como `TRIP_REQUEST_FAILED` |
| T006 | `bun run --cwd apps/frontend-transportada test` | `1864 pass · 0 fail` em 18 arquivos |
| T006 | `bun run typecheck` | as quatro apps sem erro |
| T006 | `bun run lint` | limpo nas quatro apps; a leitura da lista é tipada por `readonly unknown[]` antes do desestruturar, senão o `Array.isArray` entrega `any[]` e o `no-unsafe-assignment` reprova |
| T006 | `bunx prettier --check .` | os sete arquivos tocados pela task passam; os três `specs/05{7,8}`/`060/spec.md` que a raiz reprova são de outra frente, não desta task |

| T007 | `bun pm ls --all | grep zxing` | `@zxing/library@0.23.0` com uma dependência de produção (`ts-custom-error`); a opcional `@zxing/text-encoding` não foi instalada |
| T007 | sonda de decodificação com `bun`, fora do navegador (`typeof document: undefined · typeof window: undefined`) | leu Code-128C da chave de 44 dígitos, Code-128B da chave com CNPJ alfanumérico e QR da mesma chave — o ambiente sem DOM é o do worker |
| T007 | `bun build --target browser --minify` sobre duas entradas de sonda | `MultiFormatReader` custa 350.044 B minificado / 92.211 B gzip; `Code128Reader` + `QRCodeReader` custam 109.780 B / 28.113 B gzip — a ADR fixa o caminho profundo por causa disso |
| T007 | `grep -rn 'WebAssembly\|eval(\|blob:\|createObjectURL' node_modules/@zxing/library/esm` | uma ocorrência só, `esm/browser/BrowserCodeReader.js:1083` (`URL.createObjectURL(stream)` num `video.src`) — não é worker em `blob:` e é arquivo que não importamos; o bundle estreito não tem nenhuma das duas |
| T007 | `bun run --cwd apps/frontend-transportada build` | `built in 2.66s`; `dist/content-security-policy.txt` **inalterado byte a byte** (`diff` vazio, sha256 `0ae63395…`) — o decodificador não fala com a rede e não pede diretiva nova |
| T007 | `bunx prettier --check` nos arquivos da task | `docs/adr/0042-o-leitor-de-etiqueta-nao-afrouxa-a-csp.md`, `apps/frontend-transportada/package.json` e `bun.lock` passam |

| T008 | `bun run --cwd apps/frontend-transportada test ./test/design-system.contract.test.ts` (fase vermelha) | `error: Cannot find module '@/components/ui/barcodeDecoder.service'` — o contrato existe e o primitivo não |
| T008 | `bun run --cwd apps/frontend-transportada test ./test/design-system.contract.test.ts` (fase verde) | `1876 pass · 0 fail` em 19 arquivos — ausência de `getUserMedia`, `{}` e `{mediaDevices:{}}` devolvem `{status:'unavailable'}` sem lançar, `NotAllowedError` vira `{status:'denied'}` e `NotReadableError` vira indisponibilidade; `stopCameraStream` para toda trilha e engole `undefined`; `toLuminance` devolve **um** byte por pixel (`[255,0,0,255, 255,255,255,255]` → `[64, 255]`); e a chave de 44 dígitos é lida de um Code-128C sintetizado a partir do `CODE_PATTERNS` do próprio zxing, com `typeof document === 'undefined'` |
| T008 | mesma suíte, describe de convenção | os cinco arquivos do leitor não contêm `blob:`, `createObjectURL` nem `<svg`; o hook contém `new URL(…, import.meta.url)`, `type: 'module'`, `stopCameraStream` e `terminate()`; o componente importa `@/components/ui/icon` e declara `aria-label`; `docs/frontend/barcode-scanner.md` existe e é citado pelo `CLAUDE.md` da raiz |
| T008 | `bun run --cwd apps/frontend-transportada build` com um consumidor temporário do primitivo em `src/main.tsx` (revertido em seguida) | `dist/assets/barcodeDecoder.worker-Cy8eOPup.js` (107.93 kB), **zero** `createObjectURL` dentro dele, e a referência sai como `new Worker(new URL("/assets/barcodeDecoder.worker-….js", import.meta.url))` — a metade "empacotado pelo Vite" do aceite, que sem consumidor o tree shaking apaga |
| T008 | `grep -rn createObjectURL src/` | duas ocorrências, ambas anteriores a esta spec e alheias ao leitor: `shared/archiveDownload.service.ts:10` e `nfe-workspace/pages/NfeWorkspace.page.tsx:42` (download de arquivo). É delas o `createObjectURL` que aparece no chunk `index-*.js` |
| T008 | `bun run --cwd apps/frontend-transportada build` (sem o consumidor) | `built in 2.15s`; `dist/content-security-policy.txt` **inalterado byte a byte** (`diff` vazio, sha256 `0ae63395…`) — `worker-src 'self'` e `script-src 'self'` continuam como estavam, que é o que a ADR-0042 pediu |
| T008 | `bun run --cwd apps/frontend-transportada lint` · `typecheck` | limpo. Dois ajustes que o gate cobrou: as dicas do zxing são `Map<number, unknown>` (com `Map<never, never>` o `decode` não aceita o mapa vazio) e `CODE_PATTERNS` é privado no `.d.ts`, então o contrato o alcança por `unknown` e falha em voz alta se o campo sumir do pacote |
| T008 | `bunx prettier --check` nos arquivos da task | passam depois do `--write` em `barcodeDecoder.service.ts`, no contrato e em `docs/frontend/barcode-scanner.md` |
| T009 | `bun test test/trip.contract.test.ts` (fase vermelha) | `31 pass · 8 fail` — `resolveTripLinkReference` ausente do `tripForm.service`, o hook sem `extractNfeAccessKey`/`findNfeDocumentByAccessKey`/`isCameraCapable`/`'scannedDocumentNotFound'`, o componente sem `BarcodeScanner` nem o gate `linkForm.canScan ? (`, e `detail.linkValueHint` faltando nos dois locales |
| T009 | `bun run --cwd apps/frontend-transportada test` (fase verde) | `1884 pass · 0 fail` em 18 arquivos. A chave crua, a chave em minúscula com espaço em volta e a URL `consultaRecaptcha.aspx?p=CHAVE\|2\|1\|1\|abc` dão o mesmo `{kind:'accessKey'}` canonicalizado; o UUID dá `{kind:'identifier'}`; 44 caracteres em modo `freight` continuam identificador — cálculo de frete não tem chave de acesso; `''`, `'   '` e `' '` dão `undefined`, que é o que desabilita o botão |
| T009 | mesma suíte, describe de convenção | o hook importa `isCameraCapable` de `@/components/ui/barcodeScanner.service` e o chama com `globalThis.navigator`; o componente só desenha o botão de leitura sob `linkForm.canScan ? (` — sem câmera o campo digitável fica sozinho, sem botão que não funcionaria |
| T009 | mesma suíte, paridade de locale | as oito chaves novas de `detail` (`linkValueHint`, `scan`, `scanTitle`, `scanClose`, `scanStarting`, `scanReading`, `scanDenied`, `scanUnavailable`) e `feedback.scannedDocumentNotFound` existem em `trip.locale.json` e em `trip.en.locale.json`; `test/shared/locale-accents.contract.ts` aceita o pt-BR acentuado (`câmera`, `código`, `Permissão`, `indisponível`) |
| T009 | `bun run typecheck` (quatro apps) · `bun run --cwd apps/frontend-transportada lint` | limpo. `findNfeDocumentByAccessKey` entrou no `TripController` guardado por `canManageTrips` — a busca só existe para vincular, e vincular já é `trip.manage`; a página é o único ponto de injeção (`TripDetail.page.tsx`) |
| T009 | `bunx prettier --write` nos arquivos da task | só o contrato novo mudou; os demais já saíram formatados |
| T010 | `bun test test/design-system.contract.test.ts` (vermelho) | `179 pass · 2 fail` — as duas varreduras acusam **26** consultas: 13 desktop-first (`max-width` em `index.css:1058,1156`, `companySettings:748`, `nfeWorkspace:949,960,1430`, `DueDateField:21`; `width <= 40rem` em `companyWizard:49`, `userAdministration:321`, `nfseInvoice:299`, `cteBatch:620`, `fleet:936`, `nfeWorkspace:1586`) e 13 pontos de quebra fora dos quatro (onze `48rem`, um `60rem`, um `72rem`) |
| T010 | mesma suíte, testes de comportamento | os cinco passam desde o vermelho: `max-width: 47.99rem` e `width <= 40rem` recusados como desktop-first; `min-width: 47.99rem` e `width >= 48rem` recusados pelo valor; `40rem`/`64rem`/`80rem` e a base sem consulta aceitos; `prefers-reduced-motion`, `pointer: coarse` e `prefers-color-scheme` não são largura e passam intactos |
| T010 | leitura do inventário | ⚠️ o `tasks.md` nomeava **sete** `max-width`; a varredura achou **treze** consultas desktop-first — seis usam a sintaxe de intervalo `@media (width <= 40rem)`, que é a mesma regra em outra grafia. Um contrato que casasse só a string `max-width` nasceria com um buraco de seis sites, então ele recusa as duas. T011 cresce de sete para vinte e seis consultas em catorze arquivos |
| T010 | `docs/frontend/responsive.md` + `CLAUDE.md` | a tabela dos quatro pontos (base sem consulta · `40rem` · `64rem` · `80rem`), a proibição nas duas grafias, o alvo de toque de 44px, a ausência de rolagem horizontal do corpo e as três larguras de conferência; a regra entrou no `CLAUDE.md` da raiz, e o teste de paridade cobra as duas pontas |
| T011 | `bun run --cwd apps/frontend-transportada test` | `1891 pass · 0 fail` em 18 arquivos, o contrato de T010 entre eles — as duas varreduras que acusavam 26 consultas passam sem exceção. `grep '@media.*width' src` devolve **52 consultas, todas `min-width`/`width >=`, e só nos valores 40rem, 64rem e 80rem** |
| T011 | inversão em catorze arquivos | `index.css` (a casca), `companySettings`, `companyWizard`, `userAdministration`, `nfeWorkspace`, `DueDateField`, `nfseInvoice`, `nfseSettings`, `cteBatch`, `mdfeManifest`, `billingEligibleTable`, `operationsWorkspace`, `fleet` (duas) — treze desktop-first invertidas e treze pontos fora da grade colapsados (onze `48rem` → `40rem` ou `64rem`, um `60rem` → `64rem`, um `72rem` → `64rem`) |
| T011 | conferência das três larguras por **diff de cascata computada** (`scratchpad/cascade2.py`: achata `@media` por largura, resolve cada seletor em mapa `propriedade → valor` e compara com o `HEAD`) | 375px · 768px · 1280px em todos os treze `.css` alterados. Fora da casca sobraram **duas** diferenças, ambas pretendidas: `.companySettingsHeader h1` com `max-width: 100%` em vez de `none` (era o que a consulta `max-width` já dava no celular) e o `height: auto` dos cinco diálogos, que antes só existia acima de 48rem e agora vale de 40rem para cima |
| T011 | mesma conferência, `styles/index.css` | em **1280px** a barra volta a `position: static`, `flex: 0 0 16rem`, `transform: none`, o gatilho flutuante fica `display: none` pelos dois estados da casca, o backdrop some, `> span` do contexto volta a `inline` e a identidade volta a `max-width: 12rem` — a casca de desktop é a de antes. Em **375px e 768px** ela é a gaveta: `position: fixed`, `width: min(18rem, 86vw)`, `translateX(-100%)` e `translateX(0)` sob `-open` |
| T011 | duas mudanças de comportamento **deliberadas**, e não regressões | (a) o limiar da gaveta vai de 768px para **1024px**, que é o que `web.md` §10 manda ("sidebar vira drawer abaixo de `desktop:`") — entre 768px e 1023px a barra passa a ser gaveta onde era fixa; (b) `.workspaceDeck` vira duas colunas a partir de 1024px em vez de 1152px, e a faixa densa da tabela de notas passa a cobrir 901–1023px |
| T011 | armadilha da inversão, registrada porque quase passou | consulta invertida inverte a especificidade que decide: com `max-width` os blocos eram mutuamente exclusivos e nunca colidiam; com `min-width` a regra base **e** a de desktop valem em 1280px. Por isso os `display: none` do desktop levam o prefixo de estado da casca (`.application-shell-sidebar-collapsed .ui-button.mobile-sidebar-trigger`, (0,3,0)) — sem ele o (0,2,0) da regra móvel venceria e o gatilho ficaria visível no desktop. E `transform: translateX(0)` cria bloco de contenção: o desktop precisa de `transform: none` explícito, senão o flyout e os tooltips da barra recolhida se ancoram nela |
| T011 | `test/frontend-contract.test.ts` | a asserção `expect(styles).toContain('@media (min-width: 48rem)')` era o **degrau anterior** codificado em teste — `48rem` é exatamente o que T011 tirou da grade. Virou `expect(styles).not.toContain('48rem')`; as expectativas de `40rem`, `64rem`, `80rem` e `prefers-reduced-motion` ficam como estavam |
| T011 | `bun run --cwd apps/frontend-transportada lint` · `bun run typecheck` (quatro apps) · `bunx prettier --check src test` | limpo nos três |
| T012 | `test/trip/mobile-first.contract.ts` (novo, cinco suítes) importado por `test/trip.contract.test.ts` — entrypoint já declarado no `package.json`, sem mexer no manifesto | vermelho primeiro: 2 falhas (`--touch-target` ausente do `:root`, cabeçalho em `--space-6` no celular). Depois da implementação, `bun run --cwd apps/frontend-transportada test` → **1896 pass · 0 fail · 12509 expect() · 18 arquivos** |
| T012 | o contrato lê a folha com o `@media` que envolve cada regra | sem o contexto, base e tablet colapsariam na mesma declaração e o teste passaria com o diálogo em caixa nas duas larguras. O parser empilha preludes e devolve `{selector, media, body}` |
| T012 | diálogo do portão de CT-e em tela cheia abaixo de 640px | base: `.mdfeGateOverlay { padding: 0 }` e `.mdfeGateDialog { width: 100%; height: 100%; max-height: 100vh; border: none }`. De `40rem` para cima volta a caixa: `padding: var(--space-4)`, `width: min(38rem, 100%)`, `height: auto`, `max-height: 92vh` e a borda de cobre — o mesmo padrão que os cinco diálogos da T011 já usam (`billingOverlay`/`billingDialog` em `cteBatch`) |
| T012 | alvo de toque de 44px | `--touch-target: 2.75rem` nasce no `:root` de `index.css`, e o `min-height: 2.75rem` literal do link da barra lateral passa a lê-lo — 44px passa a ter **uma** definição. `.tripShell` sobe `--control-height-compact` para o token no celular e o devolve a `var(--field-height-compact)` a partir de `40rem`: sobe de uma vez todo botão `size="sm"` da tela (limpar rascunho, criar viagem, ações da linha) e o `.iconAction` do diálogo, sem um seletor por botão |
| T012 | por que token e não `min-height` por seletor | `.ui-button-size-sm` é classe global do design system; num CSS Module um seletor nu seria hasheado e `:global` não é usado em lugar nenhum do `src`. Herdar propriedade customizada atravessa o DOM inclusive para o diálogo `position: fixed`, que é descendente de `.tripShell` (`TripDetail.page` → `TripDetail.component` → `TripMdfePendingDialog`) |
| T012 | ordem do bloco de tablet | o `@media (min-width: 40rem)` novo vai ao **fim** do arquivo, não no que já existia na linha 82: mesma especificidade decide por ordem, e as regras base do diálogo (linha 256 em diante) venceriam um bloco declarado antes delas |
| T012 | sem rolagem horizontal do corpo em 375px | `--layout-width` = `min(100% - var(--layout-gutter), 78rem)` → 343px em 375px; `.tableScroll { overflow-x: auto }` com `.dataTable { width: 100%; white-space: nowrap }` mantém a rolagem da tabela dentro do contêiner dela; `.fieldGrid` é `1fr` na base; o cabeçalho baixa de `--space-6` para `--space-4` e volta a `--space-6` de `40rem` para cima |
| T012 | `.driverChecklist` **não** é código morto | continua em `TripDetail.component.tsx` (duas vezes) e em `TripWorkspace.page.tsx`; quem virou multi-select foi só o painel de criação |
| T012 | `bun run --cwd apps/frontend-transportada lint` · `bun run typecheck` (quatro apps) · `bunx prettier --write` nos quatro arquivos (todos já formatados) | limpo nos três |
| T013 | `test/trip/scan-link.contract.ts` (novo, doze testes) importado por `test/trip.contract.test.ts` | vermelho primeiro: 12 falhas, todas na suíte nova (1896 pass · 12 fail). Depois da implementação, `bun run --cwd apps/frontend-transportada test` → **1908 pass · 0 fail · 12558 expect() · 18 arquivos** |
| T013 | como se prova sequência sem DOM | a fila vira serviço puro, `shared/tripScanQueue.service.ts`: `acceptScannedText({queue, text})` devolve `{accessKey?, queue}` e **só** preenche `accessKey` quando a leitura virou linha nova. Leitura sem chave e leitura repetida caem no mesmo `accessKey === undefined`, e o hook fica com um `return` cedo só |
| T013 | leitura duplicada não emite segunda chamada | a dobra é sobre a fila inteira, **inclusive a linha recusada**: o código fica na frente da câmera e a leitura dispara a cada quadro, então recusar de novo seria uma ida à API por quadro. `clearScanEntries` (`Limpar leituras`) é a saída do operador. Testes: chave crua, URL do portal e chave em caixa baixa não abrem segunda linha |
| T013 | recusa por nota não interrompe a sequência | `markScanEntry` troca **uma** linha e devolve a mesma referência quando a chave não está mais na lista — veredito atrasado não ressuscita linha limpa. `linkScannedKey` traduz a falha por `resolveTripFeedbackKey` e grava `issueKey` na linha, sem tocar nas vizinhas |
| T013 | a fila autoritativa é `useRef`, não o estado | duas leituras do mesmo quadro veem o mesmo instantâneo de render, e o veredito de uma nota chega enquanto outra ainda resolve — com o `useState` sozinho a segunda escrita apagava a primeira |
| T013 | o esqueleto cobre a resolução | `TripScanQueue.component.tsx` (novo) rende `SkeletonGroup` + `Skeleton` enquanto `isTripScanEntryPending(entry)` (`resolving`/`linking`), o `feedback.${entry.issueKey}` em `role="alert"` na célula da linha recusada, e `detail.scanQueueLinked` na vinculada |
| T013 | a câmera fica aberta entre leituras | o contrato lê o corpo de `acceptScan(` por contagem de chaves e exige que `setIsScannerOpen` **não** apareça ali — `closeScanner` continua com ele. Ler o arquivo inteiro passaria mesmo com o fechamento dentro do aceite |
| T013 | por que estender `useTripDocumentLinkForm` em vez de um hook novo | `typed-access-key.contract.ts` fixa `linkForm.canScan ? (` e o `BarcodeScanner` em `TripDetail`; hook próprio poria dois leitores na tela e quebraria a T009 |
| T013 | `typed-access-key.contract.ts` reapontado | as duas asserções de `extractNfeAccessKey` saíram do hook para `tripScanQueue.service.ts` (o teste virou `resolves the read to the canonical key…`): a leitura deixou de preencher o campo digitado e passou a acrescentar linha, mas a intenção — nunca gravar a URL inteira — segue verificada |
| T013 | sete rótulos nos dois locales | `detail.scanQueue{Title,KeyColumn,StatusColumn,Resolving,Linking,Linked,Clear}` em `trip.locale.json` e `trip.en.locale.json`; `locale-accents.contract.ts` segue verde |
| T013 | `.scanQueueKey` quebra a chave | 44 caracteres sem espaço empurravam a coluna de situação para fora da tela do celular: `word-break: break-all` com `var(--font-utility)` e `var(--field-font-size-compact)`. A regra entra **antes** do `@media (min-width: 40rem)` final, pela ordem que a T012 fixou |
| T013 | `bun run --cwd apps/frontend-transportada lint` · `typecheck` · `bunx prettier --check` em `src` e `test` | limpo nos três (o hook precisou de um `--write`) |
| T014 | `test/shared/mutation-invalidation.contract.ts` ganha `src/modules/trip/hooks/useTripWorkspace.hook.ts` na lista de produtores de `nfeDocumentLink` | vermelho primeiro: 1 falha (o hook não citava `invalidateMutationEffect`). Depois, `bun run --cwd apps/frontend-transportada test` → **1909 pass · 0 fail · 12560 expect() · 18 arquivos** |
| T014 | duas invalidações, não uma | `invalidate()` (as chaves **do próprio** módulo, `['trips', companyId, tripId]` e `['trips']`) continua em criar, encerrar e marcar entregue; `invalidateDocumentLink()` soma o efeito `MUTATION_EFFECT.nfeDocumentLink` e é o `onSuccess` de **vincular** e **desvincular** — as duas pontas que mexem no vínculo da nota |
| T014 | por que entregar não entra no efeito | marcar entregue muda o estado do documento **dentro** da viagem, não o vínculo da nota com lote/NFS-e; pôr o efeito ali invalidaria a listagem de notas e as duas prévias sem que nada nelas tivesse mudado |
| T014 | nenhuma chave de outro módulo importada | `TRIP_QUERY_KEY` é do próprio módulo, então o teste `nenhum hook invalida a chave de outro módulo por conta própria` segue verde sem exceção; o alcance de fora vem só do registro |
| T014 | `bun run --cwd apps/frontend-transportada lint` · `typecheck` · `bunx prettier --check` em `src` e `test` | limpo nos três |
| T015 | `CLAUDE.md` — o papel `separator` e a permissão `trip.manage` | parágrafo novo em `## api-transportada`: as rotas de escrita da viagem trocaram `fleet.manage` por `trip.manage` (`TRIP_MANAGE_POLICY`), o `separator` recebe quatro permissões e nada mais (`invoices.read`, `fleet.read`, `trip.read`, `trip.manage`), `admin` e `operator` ganharam `trip.manage` na migration `20260824184702_separator_role`, e `test/separator-role.contract.test.ts` lista as rotas alcançáveis por extenso |
| T015 | `trip.read` está no catálogo e **nenhuma rota a pede** | achado ao conferir a mudança, e ficou escrito com ⚠️: a leitura de viagem continua em `fleet.read` (`TRIP_READ_POLICY`), e quem migrar isso migra os três papéis que a carregam (`driver`, `aggregate`, `separator`). Mesma forma da `companies.manage` reservada e sem consumidor que o `CLAUDE.md` já registrava |
| T015 | `CLAUDE.md` — o filtro `accessKey` | parágrafo novo: `GET /nfe-documents?accessKey=` é filtro de listagem, não rota nova; resolve os 44 caracteres dentro do `companyId` do contexto, chave de outra empresa é **ausência, não 403** (`test/nfe-schema/document-block-tenant-safety.contract.ts`), e o padrão é o alfanumérico `^[0-9]{6}[A-Z0-9]{12}[0-9]{26}$`, nunca `\d{44}` |
| T015 | `CLAUDE.md` — a fila de leituras | parágrafo novo em `## frontend-transportada`: a leitura não preenche o campo digitável, cada chave vira linha com esqueleto e motivo ao lado; `acceptScannedText` descarta a duplicata e `markScanEntry` ignora veredito de chave que saiu da fila; o seam é puro **porque o teste desta app não tem DOM**; vincular e desvincular disparam `MUTATION_EFFECT.nfeDocumentLink` e marcar entregue não |
| T015 | `CLAUDE.md` — `camera=(self)` | parágrafo novo: `camera=()` negava a **própria** origem e derrubava `getUserMedia` antes de qualquer diálogo; `(self)` não é `*`, e `test/shared/security-headers.contract.ts` guarda os dois sentidos — falha se `camera` voltar a `()` e falha se `geolocation`/`microphone` deixarem de ser `()` |
| T015 | os dois parágrafos de design system **já estavam escritos** | `git show 76c5815 -- CLAUDE.md` mostrou que os quatro breakpoints `min-width` (com `max-width` proibido em `src/**/*.css`) e o primitivo de leitura de código de barras (ADR-0042) entraram naquele commit. Documentar de novo duplicaria a regra em dois lugares do mesmo arquivo |
| T015 | auditoria §15 — a chave lida nunca vai a log | `grep -rn "console\.\|logger\." src/modules/trip/ src/components/ui/barcode-scanner*` não devolve nada, e na API nenhum caminho de `accessKey` loga o valor. A chave carrega o CNPJ do emitente: logá-la seria PII de pessoa jurídica em nível nenhum permitido (`security.md` §1) |
| T015 | auditoria §15 — validação na fronteira | `parseAccessKey` (`nfe-documents.schema.ts`) canonicaliza (`trim` + caixa alta) **antes** de conferir `CHAVE_PATTERN`, e o que não casa é `400`, não lista vazia — recusa explícita, como `buildTaxIdSchema` faz com o documento |
| T015 | auditoria §15 — o worker do decodificador é da própria origem | a CSP declara `worker-src 'self'` **sem `blob:`**: o `@zxing/library` é referenciado por `new URL(…, import.meta.url)` e empacotado pelo Vite, nunca montado como blob em tempo de execução (ADR-0042) |
| T015 | auditoria §15 — sem stack trace, sem tenant cruzado, sem segredo novo | a feature não acrescentou variável de ambiente, não toca certificado nem XML fiscal, e o `companyId` de toda consulta continua vindo do contexto autenticado — o `accessKey` é filtro dentro dele, nunca a chave de busca |
| T015 | `docs/SECURITY.md` não precisou de entrada nova | o achado da câmera já está lá datado de 2026-08-24 (`### … a câmera passa a ser permitida à própria origem no Permissions-Policy`), com **origem** `spec 055, T003` e **o que falta:** nada em aberto. Repetir a entrada ao fechar a feature daria dois registros do mesmo achado |
| T015 | nenhum `[NEEDS CLARIFICATION]` aberto | `grep -rn "NEEDS CLARIFICATION" specs/055-o-separador-le-a-nota-com-a-camera/` não devolve nada |
| T015 | `make check` **não fecha**, e a causa não é desta spec | o gate para no primeiro passo (`format:check`) com 17 arquivos fora de formato — os quatro `snapshot.json` de migration de viagem, `trip.schema.ts`, `test/trip-schema/status.contract.ts`, a ADR-0043 e as specs 056–063 mais `PERGUNTAS-ABERTAS.md`. Todos são da spec 056 em diante, escritos nesta árvore em paralelo e **já versionados** (`b7d7b2f`, `cc9bcea`, `1efca8d`) — o `format:check` da raiz entrou vermelho no repositório antes desta task, e formatá-los aqui seria mexer em arquivo de outra feature. Nenhum arquivo da 055 aparece na lista: `bunx prettier --check` sobre os quatro arquivos novos, os oito alterados, o `CLAUDE.md` e este `evidence.md` → limpo |
| T015 | os passos seguintes rodados à mão, porque o `&&` do `check` não chega neles | `bun run lint` → limpo nas quatro apps · `bun run typecheck` → limpo nas quatro · testes **verdes nas quatro**: api **2934 pass · 15 skip · 0 fail · 12177 expect() · 120 arquivos**, worker **683 pass · 0 fail**, cron **94 pass · 0 fail**, frontend **1909 pass · 0 fail · 12560 expect() · 18 arquivos** |
| T015 | a única falha de teste que apareceu no caminho não era desta spec, e fechou sozinha | `test/database-migration/static-migration.contract.ts` cobra a lista de migrations por extenso, e por algumas horas a árvore teve `20260824204913_trip_dispatch_snapshots/` sem versionar — a migration da 056 existindo em disco antes de o contrato dela ser atualizado. Com `1efca8d` os dois entraram juntos e a suíte da API voltou a **0 fail**; nada a fazer aqui |

## Medições de campo

Duas coisas desta spec não se provam por teste automatizado, e ficam aqui datadas.

| O que                                                                     | Como                | Quando | Resultado                                                                                         |
| ------------------------------------------------------------------------- | ------------------- | ------ | ------------------------------------------------------------------------------------------------- |
| Simbologia da etiqueta do cliente (QR ou Code-128C na DANFE do modelo 55) | ler uma DANFE real  | —      | pendente                                                                                          |
| Leitura em Android (Chromium, `BarcodeDetector` nativo)                   | T008, aparelho real | —      | pendente                                                                                          |
| Leitura em iPhone (Safari, decodificador JS)                              | T008, aparelho real | —      | pendente                                                                                          |
| Tela de viagem em 375px, 768px e 1280px                                   | T012, aparelho real | —      | pendente — T011 conferiu a **casca** por diff de cascata computada; a tela de viagem em si é T012 |
